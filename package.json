{
  "devDependencies": {
    "@types/node": "^20.14.8",
    "@types/pg": "^8.11.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  },
  "scripts": {
    "init-db": "ts-node src/init.db.ts", 
    "prepare": "./prepare.sh && npm run init-db",
    "build": "tsc",
    "start": "npm run prepare && ts-node src/index.ts && docker compose -f docker-compose.yml up -d",
    "start:test": "npm run prepare && NODE_ENV=test ts-node src/index.ts && docker compose -f docker-compose.yml -f compose.testsetup.yml up -d"
  }
}
